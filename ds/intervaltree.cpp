#include <bits/stdc++.h>
using namespace std;

// Construction takes O(nlogn), queries take O(m+logn)
// where m is the number of intervals returned.

template<typename T>
struct intervaltree {



	intervaltree(vector<pair<T, T>>& intervals) {

	}
};